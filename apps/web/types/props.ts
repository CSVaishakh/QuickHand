export type headerProps = {
    buttons: Array<{ 
        buttonName: string,
        url: string
    }>;
}

export type footerProps = {
    buttons: Array<{
        buttonName: string,
        url: string
    }>;
}